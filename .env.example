# NetSimCPP Environment Variables Example
# Copy this to .env for development or .env.production for production

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Development: Use simple passwords
# Production: Generate with: openssl rand -base64 32
DB_ROOT_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
DB_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
DB_USER=netsimuser
DB_NAME=netsimdb

# ============================================
# REDIS CONFIGURATION
# ============================================
# Development: Use simple password
# Production: Generate with: openssl rand -base64 32
REDIS_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD

# ============================================
# JWT CONFIGURATION
# ============================================
# MUST be 64+ characters in production
# Generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_TO_64_CHAR_HEX_STRING
JWT_EXPIRATION_HOURS=24

# ============================================
# GRAFANA CONFIGURATION (for monitoring)
# ============================================
GRAFANA_USER=admin
GRAFANA_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD

# ============================================
# SSL/DOMAIN CONFIGURATION (production only)
# ============================================
DOMAIN_NAME=your-domain.com
LETSENCRYPT_EMAIL=your-email@example.com

# ============================================
# PASSWORD GENERATION COMMANDS
# ============================================
# For DB_ROOT_PASSWORD, DB_PASSWORD, REDIS_PASSWORD, GRAFANA_PASSWORD:
#   openssl rand -base64 32
#
# For JWT_SECRET (must be 64 characters):
#   openssl rand -hex 32
#
# Quick setup:
#   ./scripts/generate_secrets.sh
